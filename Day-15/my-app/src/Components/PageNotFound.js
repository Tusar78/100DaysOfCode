import React from "react";

const PageNotFound = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "100vh",
        display: "grid",
        placeItems: "center",
      }}
    >
      <p>This is error page</p>
    </div>
  );
};

export default PageNotFound;
